# Python
__pycache__/
*.pyc
.venv/

# Memoria local del agente (an-kla): estado vivo, no versionado
.an-kla/

# Sistema / editor
.DS_Store
*.swp

# Referencia externa: clon de la guía Skevi (no es código del
# proyecto; ver README §"Sobre skevi/")
skevi/

# Descargas locales de fuentes (la evidencia versionada es fecha+huella
# en data/fuentes.json; el binario es copia local, REQ-7 Mac mini)
data/descargas/

# Exportaciones (productos derivados reproducibles; la evidencia
# versionada vive en data/fuentes.json y data/diccionarios/)
data/exportaciones/
data/convertidos/
